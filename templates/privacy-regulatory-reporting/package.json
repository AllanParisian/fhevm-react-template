{
  "name": "privacy-regulatory-reporting",
  "version": "1.0.0",
  "description": "Privacy-preserving regulatory reporting system",
  "scripts": {
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "compile": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "fhevm": "^0.5.0",
    "dotenv": "^16.3.1"
  }
}
